const INITIAL_STATE = {
  list: [],
};

const nomineeReducer = (state = INITIAL_STATE, action) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};

export default nomineeReducer;
